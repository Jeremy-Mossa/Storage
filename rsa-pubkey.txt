# Generate a new ssh key:
# ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# Press <enter> to save the key for the default location
# Default location: /home/jbm/.ssh/id_rsa
# Set the passphrase for added security

# Start the ssh agent
# eval "$(ssh-agent -s)"

# add ssh ket to ssh agent:
# ssh-add ~/.ssh/id_rsa

# cat the id_rsa, copy it and paste it into github
# github Settings -> SSH and GPG keys -> New ssh key
# paste the key into Key field and enter a descriptive title

# Test ssh connection
# ssh -T git@github.com
